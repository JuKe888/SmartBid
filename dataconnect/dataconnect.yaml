specVersion: "v1beta"
serviceId: "smartbid"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "smartbid-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
